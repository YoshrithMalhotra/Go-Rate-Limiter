# Go Rate Limiter

It is a fast, scalable rate-limiting API service written in Go. It uses Redis as a backing store and implements the Sliding Window rate-limiting algorithm to ensure smooth and accurate traffic control.

